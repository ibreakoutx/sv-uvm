Keywords:
 Search keywords      : scenario set kinds define_scenario atm_cell_scenario.


Technical content:
 Title                : Scenario Generator.
 Objective            : To illustrate the use of `vmm_scenario_gen macro.
 Description          : This test creates 2 different scenarios by extending from the "atm_cell_scenario" class and within each of these scenarios it defines different scenario kinds. The 2 scenario objects are pushed into the "scenario_set" queue of the generator.
 Modification comments:
 Additional setup     : None


To run the example:
    Using Makefile
 clean               - To clean up the log files and executable of previous run
 comp                - To compile the design
 run                 - To run the design
 all                 - It does all the above steps.

    Using shell scripts
 clean.csh           - To clean up the log files and executable of previous run
 comp.csh            - To compile the design
 run.csh             - To run the design


Location information:
 PATH                 : $VCS_HOME/doc/examples/testbench/sv/vmm/std_lib/vmm_scenario_gen/scenario_sets_kinds
 DIAGRAMS             : None
 Cross Reference      : $VCS_HOME/doc/UserGuide/pdf/vmm_user_guide.pdf
