

Keywords:
		Search keywords: 	SV VMM vmm_object get_namespaces create_namespace  

Technical content:
 
		Title: 		 VMM_OBJECT_NAMESPACE 
 
		Objective:     Usage of various vmm_object methods.

		Description:   This test shows the usage of the following vmm_object methods.
		               - create_namespace()
		               - get_namespaces()
																	
		Additional setup: None	
 
 To run the example:
      Using Makefile:
       	clean           -- To clean up the log files and executable of previous run
       	comp            -- To compile the design
        run             -- To run the design
        all             -- It does all the above steps.
     Using shell scripts:
        clean.csh       -- To clean up the log files and executable of previous run
        comp.csh        -- To compile the design
        run.csh         -- To run the design  
 
 
Location Information:
		PATH: 			$VMM_HOME/examples/testbench/sv/vmm/std_lib/vmm_object/nmae_space/vmm_object_namespace
		DIAGRAMS: 		None
		Cross Reference:	$VMM_HOME/doc/vmm_user_guide.pdf
