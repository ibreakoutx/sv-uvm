

Keywords:
		Search keywords: 	RAL VMM1.2 get_n_top get_n_tops namespace 

Technical content:
 
		Title: 		 VMM1.2 RAL  get_n_top get_n_tops namespace 
 
		Objective:  Demonstrate the usage of new methods for vmm_ral_block_or_sys.

		Description:  Demonstrates the usage of the following new methods :
															 1. get_n_tops : Return the number of top-level block or system RAL models.
																2. get_top    : Return the specified top-level block or system RAL model.
																This test also demonstrates the searching of the objects in 'RAL' namespace.
																
		Additional setup: None	
 
 To run the example:
      Using Makefile:
       	clean           -- To clean up the log files and executable of previous run
       	comp            -- To compile the design
        run             -- To run the design
        all             -- It does all the above steps.
     Using shell scripts:
        clean.csh       -- To clean up the log files and executable of previous run
        comp.csh        -- To compile the design
        run.csh         -- To run the design  
 
 
Location Information:
		PATH: 			$VCS_HOME/doc/examples/testbench/sv/vmm/applications/vmm_ral/get_top_namespace
		DIAGRAMS: 		None
		Cross Reference:	$VCS_HOME/doc/UserGuide/pdf/ral_user_guide.pdf
