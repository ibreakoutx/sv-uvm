

Keywords:
		Search keywords: virtual interface

Technical content:
 
		Title: 	 extern task/function through virtual interface	 
 
		Objective:  Calling of extern task/function through virtual interface	

		Description: 
		
                Same extern subroutine of different interface instances can be defined in different modules
                Different extern subroutine of same interface instance can be defined in different modules
               Any extern routine of an interface instance cannot be defined in more than one module
 
		
		Additional setup: None	
 
 To run the example:
      Using Makefile:
       	clean           -- To clean up the log files and executable of previous run
       	comp            -- To compile the design
        run             -- To run the design
        all             -- It does all the above steps.
     Using shell scripts:
        clean.csh       -- To clean up the log files and executable of previous run
        comp.csh        -- To compile the design
        run.csh         -- To run the design  
 
 
Location Information:
		PATH: 			$VCS_HOME/doc/examples/testbench/sv/ExternTaskFunCallInVirtIntf
		DIAGRAMS: 		None
	
