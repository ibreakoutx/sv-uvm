
Keywords:
  Search keywords: sv dpi unpacked struct 
 
Technical content: 

  Title: UNPACKED STRUCT DPI EXAMPLE
  
  Objective:  This example shows unpacked struct argument in DPI.
  
  Description:  This example shows unpacked struct argument in DPI.
                A user defined struct data type is defined in both SystemVerilog and C/C++ codes.  
  														
  Additional setup: None	
 
 To run the example:
      Using Makefile:
  clean           -- To clean up the log files and executable of previous run
  comp            -- To compile the design
  run             -- To run the design
  all             -- It does all the above steps.
     Using shell scripts:
  clean.csh       -- To clean up the log files and executable of previous run
  comp.csh        -- To compile the design
  run.csh         -- To run the design  
 
                
	Location Information:
  PATH:                   $VCS_HOME/doc/examples/testbench/sv/dpi_structure
  DIAGRAMS:               None
  Cross Reference:        $VCS_HOME/doc/UserGuide/vcs_ug.pdf

