

Keywords:
		Search keywords: 	vmm1.2 SV VMM vmm_opts get_bit get_int get_string option val for specific hierachical path only
		 
Technical content:
 
		Title: 		 VMM_OPTS test for get_bit/get_int/get_string for specific hierachical path only
 
		Objective:     Usage for get_bit/get_int/get_string for specific hierachical path only.
		
		Description:   This test shows how one can set option values from command line for for get_bit/get_int/get_string for specific hierachical path only.

		Additional setup: None	
 
 To run the example:
      Using Makefile:
       	clean           -- To clean up the log files and executable of previous run
       	comp            -- To compile the design
        run             -- To run the design
        all             -- It does all the above steps.
     Using shell scripts:
        clean.csh       -- To clean up the log files and executable of previous run
        comp.csh        -- To compile the design
        run.csh         -- To run the design  
 
 
Location Information:
		PATH: 			$VCS_HOME/doc/examples/testbench/sv/vmm/std_lib/vmm_opts/hierarchical_options/option_val_specific_obj
		DIAGRAMS: 		None
