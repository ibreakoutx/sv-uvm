

Keywords:
		Search keywords: 	vmm1.2 SV VMM vmm_opts get_help 

Technical content:
 
		Title: 		 VMM_OPTS test for get_help() 
 
		Objective:     Usage of vmm_opts::get_help() method.

		Description:   This test shows the usage of vmm_opts::get_help() method. This method displays the known options used. In case the same option is specified from more than one source i.e from command line as well as options file, then get_help() method will display the options as per the precedence.

		Additional setup: None	
 
 To run the example:
      Using Makefile:
       	clean           -- To clean up the log files and executable of previous run
       	comp            -- To compile the design
        run             -- To run the design
        all             -- It does all the above steps.
     Using shell scripts:
        clean.csh       -- To clean up the log files and executable of previous run
        comp.csh        -- To compile the design
        run.csh         -- To run the design  
 
 
Location Information:
		PATH: 			$VCS_HOME/doc/examples/testbench/sv/vmm/std_lib/vmm_opts/hierarchical_options/display_options_with_get_help
		DIAGRAMS: 		None
