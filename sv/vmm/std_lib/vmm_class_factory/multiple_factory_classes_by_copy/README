

Keywords:
		Search keywords:  create_by	vmm1.2

Technical content:
 
		Title: 	 	mutiple call to create_by 
 
		Objective:    replacing the userdefined blueprint through various factories

		Description:   This test shows the usage of replacing the user defined classes blueprint through various userdefined factories using create_by_copy and create_by_new 

		Additional setup: None	
 
 To run the example:
      Using Makefile:
       	clean           -- To clean up the log files and executable of previous run
       	comp            -- To compile the design
        run             -- To run the design
        all             -- It does all the above steps.
     Using shell scripts:
        clean.csh       -- To clean up the log files and executable of previous run
        comp.csh        -- To compile the design
        run.csh         -- To run the design  
 
 
Location Information:
		PATH: 			$VCS_HOME/doc/examples/testbench/sv/vmm/std_lib/vmm_class_factory/multiple_factory_classes_by_copy
		DIAGRAMS: 		None

