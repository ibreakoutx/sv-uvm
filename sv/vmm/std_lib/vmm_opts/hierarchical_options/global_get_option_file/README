

Keywords:
		Search keywords: 	vmm1.2 SV VMM vmm_opts get_bit get_int get_string globally option value file
		 
Technical content:
 
		Title: 		 VMM_OPTS test for get_bit/get_int/get_string by passing option values through options file
 
		Objective:     Usage for get/set the VMM option values from options file. 
		Description:   This test shows how one can set option values through options file for which the corresponding get_bit/get_int/get_string is called in the code.

		Additional setup: None	
 
 To run the example:
      Using Makefile:
       	clean           -- To clean up the log files and executable of previous run
       	comp            -- To compile the design
        run             -- To run the design
        all             -- It does all the above steps.
     Using shell scripts:
        clean.csh       -- To clean up the log files and executable of previous run
        comp.csh        -- To compile the design
        run.csh         -- To run the design  
 
 
Location Information:
		PATH: 			$VCS_HOME/doc/examples/testbench/sv/vmm/std_lib/vmm_opts/hierarchical_options/global_get_option_file
		DIAGRAMS: 		None

