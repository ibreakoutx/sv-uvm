

Keywords:
		Search keywords: 	SV VMM vmm_xactor Slave reactive Transcator 
 
Technical content:
 
		Title:    Writing Command-Layer Slave Transactors
 
		Objective: 	This primer is designed to learn how to write VMM-compliant command-layer slave transactorstransactors that react to pin wiggling and supply the necessary response back.
		Description: Please refer to the VMM Primer: "Writing Command-Layer Slave Transactors" in the $VCS_HOME/doc/UserGuide area

		Additional setup: None	
 
 To run the example:
      Using Makefile:
       	clean           -- To clean up the log files and executable of previous run
       	comp            -- To compile the design
        run             -- To run the design
        all             -- It does all the above steps.
     Using shell scripts:
        clean.csh       -- To clean up the log files and executable of previous run
        comp.csh        -- To compile the design
        run.csh         -- To run the design  
 
 
Location Information:
		PATH: 			$VCS_HOME/doc/examples/testbench/sv/vmm/std_lib/vmm_xactor/Command_Slave_Xactor
		DIAGRAMS: 		None
		Cross Reference:	$VCS_HOME/doc/UserGuide/pdfslave_xactor_primt.pdf
